---
import Base from '../layouts/Base.astro';
import { Picture } from 'astro:assets';
import hero from '../assets/images/me2.jpg';

const experience = [
  {
    period: '2025 · now',
    role: 'Data Scientist',
    company: 'InSilicoTrials Technologies',
    summary:
      "Synthetic data generation, agentic systems built on the Model Context Protocol, explainable AI, and Azure deployments. I also help turn rough ideas from clients into POCs that actually run.",
    tags: ['Agentic AI', 'MCP', 'Azure', 'Explainable AI', 'Synthetic Data'],
  },
  {
    period: '2024 · 2025',
    role: 'Junior Data Scientist',
    company: 'InSilicoTrials Technologies',
    summary:
      'Trained and shipped models on clinical data, wrote internal Python packages that took friction out of day-to-day ML work, and ran coding interviews and onboarding for new data scientists.',
    tags: ['Python', 'ML', 'Internal tooling'],
  },
  {
    period: '2023 · 2024',
    role: 'Junior Data Scientist',
    company: 'Miraitek',
    summary:
      'RAG pipelines on AWS, anomaly-detection algorithms for industrial signals, and BI dashboards driven by SQL for manufacturing analytics.',
    tags: ['RAG', 'AWS', 'Anomaly detection', 'SQL'],
  },
];

const projects = [
  {
    period: '2025',
    title: 'LLM multi-agent systems',
    summary:
      'Designed and contributed to several LLM systems, including a multi-agent setup with a Model Context Protocol server exposing specialised tools. Contributed upstream to the open-source mcpadapt library.',
    tags: ['LLM', 'RAG', 'MCP', 'LlamaIndex'],
    links: [
      { label: 'Model Context Protocol', href: 'https://www.anthropic.com/news/model-context-protocol' },
      { label: 'mcpadapt', href: 'https://github.com/grll/mcpadapt' },
    ],
  },
  {
    period: '2024',
    title: 'FDA challenge, top-5 finish',
    summary:
      "Placed in the top five of an FDA precisionFDA challenge, representing my company on a RAG solution evaluated against regulatory standards.",
    tags: ['Python', 'RAG', 'Regulatory'],
    links: [
      { label: 'Challenge results', href: 'https://precision.fda.gov/challenges/34/results' },
    ],
  },
  {
    period: '2024',
    title: 'POC studio',
    summary:
      'Built end-to-end proofs of concept for clients across manufacturing and life sciences. Most of the work was translating vague problem statements into minimum viable products that stakeholders could actually click on.',
    tags: ['Python', 'Streamlit', 'Docker'],
  },
];

const certs = [
  {
    year: 'Aug 2024',
    title: 'Deploying Machine Learning Models in Production',
    issuer: 'DeepLearning.AI',
    href: 'https://www.coursera.org/account/accomplishments/verify/OC06EOAHREQU',
    credentialLabel: 'OC06EOAHREQU',
  },
  {
    year: 'Aug 2024',
    title: 'Introduction to Retrieval Augmented Generation',
    issuer: 'Duke University',
    href: 'https://www.coursera.org/account/accomplishments/verify/UT2G2PAI109Z',
    credentialLabel: 'UT2G2PAI109',
  },
  {
    year: 'May 2024',
    title: 'Machine Learning in Production',
    issuer: 'DeepLearning.AI',
    href: 'https://www.coursera.org/account/accomplishments/verify/96CN9B7M28XA',
    credentialLabel: '96CN9B7M28XA',
  },
  {
    year: 'Nov 2021',
    title: 'Huawei Seeds For The Future, finalist',
    issuer: 'Huawei',
    href: 'https://magazine.unibo.it/archivio/2021/12/17/team-con-studenti-unibo-vincitore-al-seeds-for-the-future',
    credentialLabel: 'Read the article',
  },
];

const sideQuests = [
  {
    label: 'Sourdough',
    body:
      'Controlled fermentation dressed up as a hobby. Bread, pizza, anything involving yeast: irrationally into it.',
  },
  {
    label: 'Reading',
    body: 'Usually mid-book on something I can\'t put down. The current rotation lives on the',
    linkHref: '/recommended-reads/',
    linkLabel: 'reading shelf',
    bodyTail: '.',
  },
  {
    label: 'Manga',
    body: 'Occasional deep dives I will regret the next morning, and recommend the morning after that.',
  },
  {
    label: 'Outdoors',
    body: 'Pretending I have a balanced life. Hiking, long walks, anywhere with a view.',
  },
];
---
<Base
  title="Valerio Valente, Data Scientist and AI Engineer"
  description="Personal site of Valerio Valente, a data scientist and AI engineer based in Italy. I build AI systems that survive contact with reality: MLOps, LLM apps, and production ML pipelines."
  ogType="profile"
  person={true}
>
  <article class="page">
    {/* ---------- Hero ---------- */}
    <section id="top" class="hero">
      <div class="hero__photo">
        <Picture
          src={hero}
          alt="Valerio Valente, portrait"
          widths={[320, 640]}
          sizes="(max-width: 720px) 220px, 320px"
          formats={['avif', 'webp']}
          fallbackFormat="jpg"
          loading="eager"
          fetchpriority="high"
          pictureAttributes={{ class: 'hero__picture' }}
          class="hero__img"
        />
      </div>

      <div class="hero__prose">
        <h1 class="display">
          Valerio Valente<span class="display-stop">.</span>
        </h1>
        <p class="hero__lede">
          I build AI systems that survive contact with reality.
        </p>
        <div class="prose">
          <p>
            If your idea of fun is digging into the messy details instead of waving hands at slide
            decks, we are already friends. I spend most of my time turning data and machine learning
            into things that actually work outside a Jupyter notebook, which is where most "AI"
            quietly goes to die.
          </p>
          <p>
            When I am not doing that, I am either outside pretending I have a balanced life, reading
            something I cannot put down, or deep into a manga binge I will regret the next morning.
            I also bake sourdough, which is controlled fermentation dressed up as a hobby.
          </p>
        </div>
        <div class="hero__actions">
          <a href="#contact" class="btn-primary">Get in touch</a>
          <a
            href="/files/valerio_valente_resume.pdf"
            download="valerio_valente_resume.pdf"
            target="_blank"
            rel="noopener noreferrer"
            class="btn-ghost"
          >
            Download resume
          </a>
        </div>
      </div>
    </section>

    {/* ---------- Experience ---------- */}
    <section id="experience" class="section">
      <header class="section__head">
        <p class="section-eyebrow">Experience</p>
        <h2 class="section-title">Where I have worked, in reverse order.</h2>
      </header>

      <ol class="entries" role="list">
        {experience.map((entry) => (
          <li class="entry">
            <p class="marginalia marginalia--year entry__year">{entry.period}</p>
            <div class="entry__body">
              <h3 class="entry__title">
                {entry.role}
                <span class="entry__company">
                  <span aria-hidden="true">&nbsp;&middot;&nbsp;</span>
                  {entry.company}
                </span>
              </h3>
              <p class="entry__summary">{entry.summary}</p>
              {entry.tags && entry.tags.length > 0 && (
                <p class="entry__tags marginalia">
                  {entry.tags.map((t, idx) => (
                    <>
                      {idx > 0 && (
                        <span aria-hidden="true">&nbsp;&middot;&nbsp;</span>
                      )}
                      <span>{t}</span>
                    </>
                  ))}
                </p>
              )}
            </div>
          </li>
        ))}
      </ol>
    </section>

    {/* ---------- Selected Projects ---------- */}
    <section id="work" class="section">
      <header class="section__head">
        <p class="section-eyebrow">Selected Projects</p>
        <h2 class="section-title">A few things I am proudest of.</h2>
      </header>

      <ol class="entries" role="list">
        {projects.map((p) => (
          <li class="entry">
            <p class="marginalia marginalia--year entry__year">{p.period}</p>
            <div class="entry__body">
              <h3 class="entry__title">{p.title}</h3>
              <p class="entry__summary">
                {p.summary}
                {p.links && p.links.length > 0 && (
                  <>
                    {' ('}
                    {p.links.map((l, idx) => (
                      <>
                        {idx > 0 && ', '}
                        <a href={l.href} target="_blank" rel="noopener noreferrer">{l.label}</a>
                      </>
                    ))}
                    {')'}
                  </>
                )}
              </p>
              {p.tags && p.tags.length > 0 && (
                <p class="entry__tags marginalia">
                  {p.tags.map((t, idx) => (
                    <>
                      {idx > 0 && (
                        <span aria-hidden="true">&nbsp;&middot;&nbsp;</span>
                      )}
                      <span>{t}</span>
                    </>
                  ))}
                </p>
              )}
            </div>
          </li>
        ))}
      </ol>
    </section>

    {/* ---------- Side Quests ---------- */}
    <section id="side-quests" class="section">
      <header class="section__head">
        <p class="section-eyebrow">Side Quests</p>
        <h2 class="section-title">The hobbies are load-bearing.</h2>
      </header>

      <dl class="quests">
        {sideQuests.map((q) => (
          <div class="quest">
            <dt class="quest__label marginalia">{q.label}</dt>
            <dd class="quest__body">
              {q.body}
              {q.linkHref && (
                <>
                  {' '}<a href={q.linkHref}>{q.linkLabel}</a>{q.bodyTail}
                </>
              )}
            </dd>
          </div>
        ))}
      </dl>
    </section>

    {/* ---------- Certifications & Awards ---------- */}
    <section id="certifications" class="section">
      <header class="section__head">
        <p class="section-eyebrow">Certifications &amp; Awards</p>
        <h2 class="section-title">Receipts.</h2>
      </header>

      <ol class="entries" role="list">
        {certs.map((c) => (
          <li class="entry">
            <p class="marginalia marginalia--year entry__year">{c.year}</p>
            <div class="entry__body">
              <h3 class="entry__title">
                {c.title}
                <span class="entry__company">
                  <span aria-hidden="true">&nbsp;&middot;&nbsp;</span>
                  {c.issuer}
                </span>
              </h3>
              <p class="entry__summary marginalia">
                <a href={c.href} target="_blank" rel="noopener noreferrer">{c.credentialLabel}</a>
              </p>
            </div>
          </li>
        ))}
      </ol>
    </section>

    {/* ---------- Contact ---------- */}
    <section id="contact" class="section section--contact">
      <header class="section__head">
        <p class="section-eyebrow">Hello</p>
        <h2 class="section-title">Say hi, share a draft, suggest a book.</h2>
        <p class="prose section__intro">
          The fastest reply paths are the social ones below. The form is fine too if a longer note
          is easier. Either way, I read everything.
        </p>
      </header>

      <ul class="socials" role="list">
        <li>
          <a href="https://github.com/valeriova96" target="_blank" rel="noopener noreferrer" aria-label="GitHub profile">
            <span class="socials__label">GitHub</span>
            <span class="marginalia">/valeriova96</span>
          </a>
        </li>
        <li>
          <a href="https://www.linkedin.com/in/valerio-valente/" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn profile">
            <span class="socials__label">LinkedIn</span>
            <span class="marginalia">/valerio-valente</span>
          </a>
        </li>
        <li>
          <a href="https://medium.com/@valeriovalente96" target="_blank" rel="noopener noreferrer" aria-label="Medium profile">
            <span class="socials__label">Medium</span>
            <span class="marginalia">@valeriovalente96</span>
          </a>
        </li>
      </ul>

      <form action="https://formspree.io/f/movlqqpz" method="POST" class="contact-form" aria-labelledby="contact-heading">
        <h3 id="contact-heading" class="visually-hidden">Contact form</h3>
        <div class="contact-form__row">
          <label class="field-label" for="name">Name</label>
          <input class="field-input" type="text" name="name" id="name" required autocomplete="name" />
        </div>
        <div class="contact-form__row">
          <label class="field-label" for="email">Email</label>
          <input class="field-input" type="email" name="email" id="email" required autocomplete="email" />
        </div>
        <div class="contact-form__row">
          <label class="field-label" for="message">Message</label>
          <textarea class="field-textarea" name="message" id="message" rows="5" required></textarea>
        </div>
        <div class="contact-form__row contact-form__consent">
          <label>
            <input type="checkbox" name="privacy" required />
            <span class="marginalia">
              I agree to the <a href="/privacy/" target="_blank" rel="noopener noreferrer">privacy policy</a>.
            </span>
          </label>
        </div>
        <button type="submit" class="btn-primary">Send message</button>
      </form>
    </section>
  </article>
</Base>

<style>
  .page {
    max-width: 64rem;
    margin: 0 auto;
    padding: clamp(2rem, 4vw, 4rem) clamp(1.25rem, 3vw, 2.5rem) 0;
  }

  /* ---------- Hero ---------- */

  .hero {
    display: grid;
    grid-template-columns: 1fr;
    gap: clamp(2rem, 4vw, 3.5rem);
    align-items: start;
    padding-block: clamp(2.5rem, 5vw, 5rem);
  }

  @media (min-width: 820px) {
    .hero {
      grid-template-columns: 5fr 7fr;
      gap: clamp(3rem, 5vw, 5rem);
    }
  }

  .hero__photo {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    align-items: flex-start;
  }

  .hero__picture {
    display: block;
  }

  .hero__img {
    display: block;
    width: clamp(180px, 36vw, 320px);
    aspect-ratio: 1 / 1;
    object-fit: cover;
    border: 1px solid var(--color-rule);
    border-radius: 2px;
  }

  .hero__caption {
    max-width: 26ch;
  }

  .hero__prose {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .hero__lede {
    font-family: var(--font-serif);
    font-weight: 500;
    font-size: clamp(1.25rem, 0.5vw + 1.1rem, 1.55rem);
    line-height: 1.3;
    color: var(--color-ink);
    max-width: 36ch;
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 0.5rem;
  }

  /* ---------- Section scaffolding ---------- */

  .section {
    padding-block: clamp(3rem, 6vw, 5.5rem);
    border-top: 1px solid var(--color-rule);
  }

  .section__head {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    max-width: 52ch;
    margin-bottom: clamp(1.75rem, 3vw, 2.5rem);
  }

  .section__intro {
    margin-top: 0.25rem;
  }

  /* ---------- Entries (rows with hairline rules) ---------- */

  .entries {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .entry {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.4rem;
    padding-block: 1.5rem;
    border-bottom: 1px solid var(--color-rule);
  }

  .entries .entry:first-child {
    border-top: 1px solid var(--color-rule);
  }

  @media (min-width: 720px) {
    .entry {
      grid-template-columns: 8.5rem 1fr;
      gap: 1.5rem;
      padding-block: 1.75rem;
    }
  }

  .entry__year {
    padding-top: 0.4rem;
    color: var(--color-ink-soft);
  }

  .entry__body {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    min-width: 0;
  }

  .entry__title {
    font-family: var(--font-serif);
    font-weight: 600;
    font-size: clamp(1.15rem, 0.4vw + 1.05rem, 1.3rem);
    line-height: 1.3;
    letter-spacing: -0.005em;
    color: var(--color-ink);
  }

  .entry__company {
    font-weight: 400;
    color: var(--color-ink-soft);
  }

  .entry__summary {
    color: var(--color-ink);
    font-size: 1.025rem;
    line-height: 1.6;
    max-width: 68ch;
  }

  .entry__summary a {
    color: var(--color-terracotta);
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 0.18em;
    text-decoration-color: color-mix(in oklch, var(--color-terracotta) 55%, transparent);
    transition: color 200ms cubic-bezier(0.22, 1, 0.36, 1),
                text-decoration-color 200ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .entry__summary a:hover {
    color: var(--color-terracotta-deep);
    text-decoration-color: var(--color-terracotta-deep);
  }

  .entry__tags {
    color: var(--color-ink-soft);
  }

  .entry:hover .entry__year {
    color: var(--color-terracotta);
  }

  /* ---------- Side Quests ---------- */

  .quests {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
  }

  @media (min-width: 720px) {
    .quests {
      grid-template-columns: 1fr 1fr;
      gap: 1.75rem 3rem;
    }
  }

  .quest {
    display: grid;
    grid-template-columns: 8.5rem 1fr;
    gap: 1rem;
    padding-block: 1rem;
    border-top: 1px solid var(--color-rule);
  }

  .quest__label {
    padding-top: 0.15rem;
  }

  .quest__body {
    font-size: 1.025rem;
    line-height: 1.6;
    color: var(--color-ink);
    max-width: 48ch;
  }

  .quest__body a {
    color: var(--color-terracotta);
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 0.18em;
    text-decoration-color: color-mix(in oklch, var(--color-terracotta) 55%, transparent);
  }

  .quest__body a:hover {
    color: var(--color-terracotta-deep);
    text-decoration-color: var(--color-terracotta-deep);
  }

  /* ---------- Contact ---------- */

  .section--contact {
    padding-bottom: clamp(3rem, 6vw, 5rem);
  }

  .socials {
    list-style: none;
    margin: 0 0 2.5rem;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem 2.5rem;
    border-top: 1px solid var(--color-rule);
    border-bottom: 1px solid var(--color-rule);
    padding-block: 1.25rem;
  }

  .socials a {
    display: inline-flex;
    align-items: baseline;
    gap: 0.6rem;
    color: var(--color-ink);
    transition: color 180ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .socials a:hover {
    color: var(--color-terracotta);
  }

  .socials__label {
    font-family: var(--font-serif);
    font-weight: 600;
    font-size: 1.05rem;
  }

  /* ---------- Contact form ---------- */

  .contact-form {
    max-width: 36rem;
    display: grid;
    gap: 1.5rem;
  }

  .contact-form__row {
    display: flex;
    flex-direction: column;
  }

  .contact-form__consent label {
    display: inline-flex;
    align-items: flex-start;
    gap: 0.6rem;
    color: var(--color-ink);
  }

  .contact-form__consent input[type='checkbox'] {
    margin-top: 0.25rem;
    accent-color: var(--color-terracotta);
  }

  .contact-form__consent a {
    color: var(--color-terracotta);
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 0.18em;
  }

  .contact-form button[type='submit'] {
    justify-self: start;
  }

  .visually-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    margin: -1px;
    padding: 0;
    overflow: hidden;
    clip: rect(0 0 0 0);
    white-space: nowrap;
    border: 0;
  }
</style>
